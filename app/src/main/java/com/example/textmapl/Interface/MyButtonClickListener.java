package com.example.textmapl.Interface;

/*
    Interface pour l'evennement de click sur l'écran via les boutons pour les mouvements de gestures
    de la classe MySwipeHelper
 */
public interface MyButtonClickListener {
    void onClick(int pos);
}
